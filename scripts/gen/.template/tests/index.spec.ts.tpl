import { mount } from '@vue/test-utils'
import {{ name }} from '../src/index.vue'

describe('{{ dirname }}', () => {
  test('{{ zhName }}测试', () => {
    const wrapper = mount({{ name }})
    expect(wrapper)
  })
})